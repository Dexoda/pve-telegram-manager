version: '3.8'

services:
  pve-telegram-bot:
    build: .
    container_name: pve-telegram-bot
    network_mode: host
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ~/.ssh:/root/.ssh:ro
    environment:
      - PYTHONUNBUFFERED=1
